meta {
  name: Login as Tom bad password
  type: http
  seq: 8
}

post {
  url: {{url}}/api/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
  	"email": "tom@test.com",
  	"password": "BadPassword"
  }
}

settings {
  encodeUrl: true
}
